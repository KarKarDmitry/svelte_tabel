# HTTP — dev-режим (TLS-блок ниже активируется, когда появится сертификат)
server {
    listen 80;
    server_name _;

    # Крупные файлы импорта турникетов (XLS)
    client_max_body_size 100m;

    location / {
        proxy_pass http://app:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE-стрим статуса импорта (turnstile-import.service emitToStatus)
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
    }
}

# HTTPS — включить, когда появится сертификат (приватный CA, mettem.lan)
# server {
#     listen 443 ssl;
#     server_name mettem.lan;
#
#     ssl_certificate /etc/nginx/certs/mettem.lan.crt;
#     ssl_certificate_key /etc/nginx/certs/mettem.lan.key;
#
#     client_max_body_size 100m;
#
#     location / {
#         proxy_pass http://app:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#
#         proxy_buffering off;
#         proxy_cache off;
#         proxy_read_timeout 3600s;
#     }
# }